---

- name: My first playbook
  hosts: localhost
  gather_facts: True
  tasks:
          - name: test connectivity
            setup:
          - name: print os family
            debug:
              var:  ansible_os_family
